# File protocol

- Working copy may be versioned (`name_v01.x.html`).
- Client copy lives in `_deliverables/` with a canonical unversioned name.
- Refresh `_deliverables/` when the working copy changes.
- Re-clone is a new clone with a new source, not a silent overwrite of theme/polish work.
