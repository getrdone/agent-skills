# Source contract

One file per clone operation.

Save as `source.png` or `source.html` beside the page. That file is the contract.

If two files arrive, name one source and one inspiration. Clone the source only.
